Pada modul ini digunakan untuk menyimpan data master batch dengan detail atribut sbb.

Atribute :
- id : int autoincrement
- batch_name : varchar(30) -
- class_id : int <>
- mentor_id : int <>
- start_date : timestamp -
- end_date : timestamp -
- duration ( isian hari kegiatan bootcamp) :  ?? masih bingung
- banned : bool
- banned_time  : timestamp -
- created_time : timestamp -
- created_by : varchar(50) - 
- updated_time : timestaxmp -

Function :


- list

- create

- update

- delete

- detail

Validation :
- inputan tidak boleh kosong. -

- start date tidak boleh lebih besar dari end date.

- delete -> ketika sudah berelasi tidak dapat dihapus fan memunculkan notifikasi "Data tidak dapat dihapus karena telah berelasi dengan data lain"

GET /batch/all
GET /batch/{id}
POST /batch
PUT /batch/{id}
PATCH /batch/{id}