server.port=8880
logging.config=./logback.xml
swagger.enabled=true

spring.datasource.url=jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:kedukasi}
spring.datasource.username=${DATABASE_USER:docker}
spring.datasource.password=${DATABASE_PASSWORD:docker}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
# App Properties
bezkoder.app.jwtSecret=kedukasi2022
bezkoder.app.jwtExpirationMs=600000
